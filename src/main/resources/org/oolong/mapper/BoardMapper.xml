<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.oolong.mapper.BoardMapper">
	<insert id="insert">
		<selectKey order="AFTER" keyProperty="bno" resultType="long">
			select LAST_INSERT_ID()
		</selectKey>
		insert into tbl_board (title, content, writer)
		values (#{title}, #{content}, #{writer})
	</insert>
	<select id="selectOne" resultType="BoardDTO">
		select * from tbl_board where bno = #{bno}
	</select>
	<update id="delete">
		update tbl_board set delFlag = true where bno = #{bno}
	</update>
	<update id="update">
		update tbl_board
			set title = #{title}, content = #{content}, updateDate = now()
		where bno = #{bno}
	</update>
	<select id="list" resultType="BoardDTO">
		select bno, title, writer, regDate from tbl_board
		where delflag = false
		order by bno desc
	</select>
	<select id="list2" resultType="BoardDTO">
		select bno, title, writer, regDate
		from tbl_board
		where delFlag = false
		order by bno desc
		limit #{count} offset #{skip}
	</select>
	<select id="listCount">
		select count(bno) from tbl_board
		where delFlag = false
	</select>
	
	<sql id="search">
		<if test="types != null and keyword != null">
			<foreach collection="types" item="type" open=" and (" close=")" separator=" or ">
				<if test='type.equals("T")'>
					title like CONCAT('%', #{keyword}, '%')
				</if>
				<if test='type.equals("C")'>
					content like CONCAT('%', #{keyword}, '%')
				</if>
				<if test='type.equals("W")'>
					writer like CONCAT('%', #{keyword}, '%')
				</if>
			</foreach>
		</if>
	</sql>
	
	<select id="listSearch" resultType="BoardDTO">
		select board.bno, title, writer, board.regDate, count(rno) replyCnt
		from tbl_board board left outer join tbl_reply reply on board.bno = reply.bno
		where board.delFlag = false
			
		<include refid="search"></include>
		
		group by board.bno
		order by bno desc
		limit #{count} offset #{skip}
	</select>
	
	
	<select id="listCountSearch">
		select count(bno) from tbl_board
			where delFlag = false
		<include refid="search"></include>
	</select>
	
</mapper>